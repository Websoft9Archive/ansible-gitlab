common_os_support: ["CentOS", "Ubuntu", "Amazon"]

docker_applications: 
  - portainer
  

init_application:
  gitlab:
    username: root
    password: 123456
    commands: 
      - cd /tmp
      - cd /tmp/;sudo python3 ./change_gitlab_password.py $new_password
      - sudo sleep 50
      - sudo rm ./change_gitlab_password.py
 
