common_os_support: ["CentOS", "Ubuntu", "Amazon"]

docker_applications: 
  - portainer
  

init_application:
  gitlab:
    username: root
    password: 12345678
    commands: 
      - sudo echo $new_password
      - sudo cd /tmp;sudo python3 change_gitlab_password.py $new_password;sleep 30;sudo rm change_gitlab_password.py;
 
