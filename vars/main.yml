common_os_support: ["CentOS", "Ubuntu", "Amazon"]

docker_applications: 
  - portainer
  

init_application:
  gitlab:
    username: root
    password: 12345678
    commands: 
      - cd /tmp;python3 change_gitlab_password.py $new_password;rm change_gitlab_password.py;
 
